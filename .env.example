# ========================================================
# ApexBridge Production & Development Environment Config
# ========================================================

# MongoDB Atlas Connection URI
# Replace <db_password> with your MongoDB user's actual password:
MONGODB_URI=mongodb+srv://henrydavid1602_db_user:<db_password>@cluster0.kyzqq0m.mongodb.net/apexbridge?retryWrites=true&w=majority&appName=Cluster0

# Server Port (Render sets this automatically in production)
PORT=4000

# Environment Mode
NODE_ENV=development

# JWT Secret for signing investor and admin session tokens
JWT_SECRET=apexbridge_super_secret_jwt_key_2026_x89f_prod_secure

# Super Admin Seed Credentials
ADMIN_EMAIL=admin@apexbridge.com
ADMIN_PASSWORD=AdminPassword123!

# Allowed CORS Origins (set to * or your frontend domain)
CORS_ORIGIN=*
